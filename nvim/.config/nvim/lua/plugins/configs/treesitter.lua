local options = {
  ensure_installed = {
        "lua",
        "go",
        "gomod",
        "rust",
        "json",
        "yaml",
        "dockerfile",
  },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options